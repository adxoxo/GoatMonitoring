from django.apps import AppConfig


class GoatsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.goats"
    verbose_name = "Goats — registry, lineage, pens"
